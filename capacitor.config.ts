import type { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'com.koumy.sermons',
  appName: 'Bibliothèque de Sermons',
  webDir: 'dist',
};

export default config;
